后台管理功能增强计划
任务 0：Loading 骨架屏演示 HTML
创建一个独立的 skeleton-demo.html 演示文件，让你直观理解骨架屏是什么
包含：文章卡片骨架屏、统计卡片骨架屏、表格骨架屏、列表骨架屏四种常见模式
纯 HTML+CSS，可直接在浏览器打开查看效果
任务 1：前端文章封面完善
文件： src/views/article/Edit.vue
当前 ImageUpload 组件已存在且功能完善（支持拖拽上传、预览、替换、删除）
在发布设置弹窗中已集成了封面图选择
改进点： 在文章列表 List.vue 中展示封面缩略图，目前列表完全不显示封面
文件： src/views/article/List.vue
文章卡片中增加封面图缩略图（左侧小图 + 右侧信息布局）
没有封面的文章显示默认占位图
任务 2：全局 Loading 骨架屏
文件（新建）： src/components/common/SkeletonLoader.vue
通用骨架屏组件，支持多种类型：card（卡片）、list（列表行）、stats（统计）、table（表格）
使用 CSS 动画实现脉冲闪烁效果（shimmer animation）
文件修改：
List.vue — 文章列表加载时显示骨架屏
dashboard/Index.vue — 仪表盘统计加载时显示骨架屏
comment/List.vue — 评论列表加载时显示骨架屏
category/List.vue — 分类列表加载时显示骨架屏
tag/List.vue — 标签列表加载时显示骨架屏
link/List.vue — 友链列表加载时显示骨架屏
任务 3：文章定时发布
后端修改：
entity/article.go — 添加 ScheduledAt 字段（*time.Time），新增 scheduled 状态
request/article.go — Create/Update 请求添加 scheduled_at 字段
数据库迁移 SQL — ALTER TABLE articles ADD COLUMN scheduled_at DATETIME NULL
前端修改：
Edit.vue — 发布设置弹窗中增加"定时发布"日期时间选择器，支持选择未来时间
任务 4：文章 SEO 管理
后端修改：
entity/article.go — 添加 SEOTitle、SEODescription、SEOKeywords 三个字段
request/article.go — Create/Update 请求添加 SEO 字段
数据库迁移 SQL — ALTER TABLE articles ADD COLUMN seo_title VARCHAR(200) DEFAULT '', ADD COLUMN seo_description VARCHAR(500) DEFAULT '', ADD COLUMN seo_keywords VARCHAR(300) DEFAULT ''
前端修改：
Edit.vue — 发布设置弹窗中增加"SEO 设置"折叠区域，包含：SEO 标题、SEO 描述、SEO 关键词三个输入框
任务 5：文章 Slug 自定义
后端： entity 已有 Slug 字段，但 Create/Update 请求缺少该字段
request/article.go — Create/Update 请求添加 slug 字段
article_service.go — 创建时自动生成 slug（如果为空则从标题生成拼音/英文 slug），更新时支持自定义
前端修改：
Edit.vue — 发布设置弹窗中增加"URL Slug"输入框，显示完整 URL 预览
任务 6：操作日志/审计
后端（新建）：
internal/model/entity/audit_log.go — 审计日志实体（操作人、操作类型、目标类型、目标ID、详情、IP、时间）
internal/repository/audit_log_interface.go + audit_log_repository.go — 日志仓库
internal/service/audit_log_service.go — 日志服务
internal/api/v1/audit_log/controller.go + routes.go — 日志查询 API
数据库迁移 SQL — 创建 audit_logs 表
在关键操作（创建文章、更新文章、删除文章、审核评论等）中记录日志
前端（新建）：
src/views/audit/Index.vue — 操作日志查看页面
src/api/audit.js — 日志 API 封装
router/index.js — 添加 /audit 路由
AdminLayout.vue — 侧边栏增加"操作日志"菜单项
任务 7：SQL 迁移脚本
创建 migration.sql 文件，包含所有新增字段的 ALTER TABLE 语句
创建 audit_logs 表的 CREATE TABLE 语句
执行顺序
任务 0 → 任务 1 → 任务 2 → 任务 5 → 任务 3+4 → 任务 6 → 任务 7
（先做不需要后端改动的纯前端任务，再做需要后端配合的任务）